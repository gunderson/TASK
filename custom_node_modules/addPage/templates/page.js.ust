/* exlint-env es6 */
var _ = require( 'lodash' );
var TASK = require( '_TASK/TASK-Base' );
var TaskPage = require( '_TASK/views/pages/Page' );

class <%=name%>Page extends TaskPage {
	constructor( options ) {

		// ---------------------------------------------------
		// Local Properties

		super( _.defaults( options, {
			name: '<%=name%>',
			el: null,
			col: 0,
			row: 0,
			type: 'page',
			views: null,
			events: null
		} ) );

		// ---------------------------------------------------
		// Event Listeners

		// ---------------------------------------------------
		// Function Bindings

		// ---------------------------------------------------
		// Local Properties
	}

	// ---------------------------------------------------
	// Public Methods

	// ---------------------------------------------------
	// Event Handlers

	// ---------------------------------------------------
	// Getters & Setters
}

module.exports = <%=name%>Page;
